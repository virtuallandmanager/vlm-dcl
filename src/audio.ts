export const createAudioStream = (audioStreams: Array<any>) => {
  if (!audioStreams) {
    return;
  }
};

export const updateAudioStream = (audioStreams: Array<any>, property: string, id: string) => {};

export const deleteAudioStream = (id: string) => {};
