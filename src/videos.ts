import * as vs from "dcl-video-system";

export const videoSystems: any = {};

export const initVideoScreens = (videoConfigs: Array<vs.VideoSystemConfig>) => {
  for (let i = 0; i < videoConfigs.length; i++) {
    
  }
};

export const createVideoScreen = (videoConfig: vs.VideoSystemConfig) => {};

export const updateVideoScreen = (videoConfig: vs.VideoSystemConfig, property: string) => {};

export const removeVideoScreen = (videoConfig: vs.VideoSystemConfig, property: string) => {};
